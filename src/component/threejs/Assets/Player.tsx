import React from 'react';
import {useSpring, animated} from '@react-spring/three'

const Player:React.FC<{props:any}>=({...props})=>{
    <sprite>
      <spriteMaterial map={texture} />
    </sprite>
}

export default Player;
